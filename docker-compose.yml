services:
  frontend:
    build:
      context: https://github.com/gonzabgarcia/frontend-qa-automation
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
      interval: 10s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M

  cypress:
    image: cypress/included:13.13.1
    depends_on:
      frontend:
        condition: service_healthy
    volumes:
      - ./e2e:/e2e
    working_dir: /e2e
    entrypoint: ["npx", "cypress", "run"]
    environment:
      - CYPRESS_baseUrl=http://frontend:3000
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M